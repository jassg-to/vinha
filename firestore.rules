rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all direct client access — all Firestore access goes through the backend API
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
